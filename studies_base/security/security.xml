<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <!-- This file should contain only security objects

        <record model="res.groups" id="base.group_seintinelles_contact_base_user">
            <field name="name">seintinelles_contact_base / User</field>
        </record>

        <record model="res.groups" id="base.group__manager">
            <field name="name"> / Manager</field>
            <field name="implied_ids" eval="[(4, ref('base.group_seintinelles_contact_base_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
        </record>

        <record id="seintinelles_contact_base_users_access" model="ir.rule">
            <field name="name">Access to seintinelles_contact_base for regular users</field>
            <field name="model_id" ref="seintinelles_contact_base.model_seintinelles_contact_base_seintinelles_contact_base"/>
            <field name="domain_force">[('stage_id.closed', '=', False)]</field>
            <field name="groups" eval="[(4, ref('base.group_seintinelles_contact_base_user'))]"/>
            <field eval="0" name="perm_unlink"/>
            <field eval="0" name="perm_write"/>
            <field eval="1" name="perm_read"/>
            <field eval="0" name="perm_create"/>
        </record>

        -->


    </data>
</odoo>
