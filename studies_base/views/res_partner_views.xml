<?xml version="1.0" ?>
<odoo>
    <record id="view_partner_study_form" model="ir.ui.view">
        <field name="name">view_partner_study_form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <div class="oe_button_box" name="button_box">
                <button
                    context="{'search_default_open': True, 'default_partner_id': active_id}"
                    name="action_view_partner_study"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-search"
                    >
		    <field string="Études" name="study_count" widget="statinfo"/>
                </button>
		<button
                    context="{'search_default_open': True, 'default_partner_id': active_id}"
                    name="action_view_partner_study_participants"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-search"
                    >
		    <field string="Participations" name="study_participant_count" widget="statinfo"/>
                </button>
            </div>
        </field>
    </record>
</odoo>
